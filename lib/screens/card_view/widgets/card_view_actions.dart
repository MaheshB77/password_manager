import 'package:flutter/material.dart';

class CardViewActions extends StatelessWidget {
  const CardViewActions({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}